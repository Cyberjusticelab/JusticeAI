version: '3'

services:
  ml_service:
    command: bash -c "pytest && flake8 --ignore F403,F405,E501"
  nlp_service:
    command: bash -c "pytest && flake8 --ignore F403,F405,E501"
  backend_service:
    command: bash -c "pytest && flake8 --ignore F403,F405,E501"
  web_client:
    command: bash -c "npm run test && npm run lint"
  postgresql_db:
    environment:
      POSTGRES_PASSWORD: DEV_PASS_NOT_SECRET
